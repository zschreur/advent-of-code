//! Day 0 is a test puzzle for setting up Advent of Code in preparation for the games to begin.
const std = @import("std");

pub fn partOne(input_reader: anytype) !u64 {
    _ = &input_reader;
    return error.NotImplemented;
}

pub fn partTwo(input_reader: anytype) !u64 {
    _ = &input_reader;
    return error.NotImplemented;
}

const sample_input =
    \\hello
    \\world
;

const testing = @import("std").testing;
test "part one" {}

test "part two" {}
